<h1>Book a Lesson</h1>
<%= form_for(@user) do |f| %>
  <%= f.label :date
  <%= f.text_field :date, placeholder: 'Enter date' %> %>
  <%= f.submit %>

  <%= f.label :time %>
  <%= f.text_field :time, placeholder: 'Enter time' %> %>
  <%= f.button :submit %>

<% end %>
